<?xml version="1.0" encoding="UTF-8"?>
<!-- AUTO-GENERATED Maven harness for the h2-1.3-to-2.0-sql-compat BBC differential.
     Compiles the target repo's untouched production source and runs ONE authored
     test; the dependency version is parameterised via -Ddep.version. -->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>bbc.harness</groupId>
  <artifactId>h2-bbc-harness</artifactId>
  <version>1.0</version>
  <properties>
    <maven.compiler.release>11</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <dep.version>2.0.206</dep.version>
  </properties>
  <dependencies>
    <dependency>
      <groupId>com.h2database</groupId>
      <artifactId>h2</artifactId>
      <version>${dep.version}</version>
    </dependency>
    <dependency>
      <groupId>junit</groupId><artifactId>junit</artifactId>
      <version>4.13.2</version><scope>test</scope>
    </dependency>
    <!-- TODO(JUDGMENT): add the repo's OTHER compile deps so src/main compiles
         (copy from its pom.xml / build.gradle). -->
  </dependencies>
  <build>
    <sourceDirectory>src/main/java</sourceDirectory>
    <testSourceDirectory>src/test/java</testSourceDirectory>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId><version>3.2.5</version>
        <configuration>
          <includes><include>**/H2BbcTest.java</include></includes>
          <argLine></argLine>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
