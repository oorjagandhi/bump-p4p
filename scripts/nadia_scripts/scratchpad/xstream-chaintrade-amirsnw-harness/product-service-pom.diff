diff --git a/product-service/pom.xml b/product-service/pom.xml
index 131691e..1fb8bc4 100644
--- a/product-service/pom.xml
+++ b/product-service/pom.xml
@@ -12,7 +12,18 @@
     <artifactId>product-service</artifactId>
     <name>product-service</name>
 
+    <properties>
+        <bbc.xstream.version>1.4.20</bbc.xstream.version>
+    </properties>
+
     <dependencies>
+        <!-- BBC harness: explicit depth-1 xstream so the version override actually applies
+             (otherwise xstream is pinned transitively by Axon). Not an app change. -->
+        <dependency>
+            <groupId>com.thoughtworks.xstream</groupId>
+            <artifactId>xstream</artifactId>
+            <version>${bbc.xstream.version}</version>
+        </dependency>
         <dependency>
             <groupId>org.springframework.boot</groupId>
             <artifactId>spring-boot-starter-web</artifactId>
